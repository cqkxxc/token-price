---
interface Props { active?: 'home' | 'monitor'; }
const { active = 'home' } = Astro.props;
---
<nav class="topnav" id="topNav" aria-label="主导航"><div class="topnav-inner">
  <a class="topnav-brand" href="/"><div class="topnav-brand-dot">AI</div>AI 模型比价</a>
  <div class="topnav-links">
    <a class={`topnav-link ${active === 'home' ? 'active' : ''}`} href="/">首页</a>
    <a class={`topnav-link ${active === 'monitor' ? 'active' : ''}`} href="/monitor/">监控</a>
  </div>
</div></nav>
